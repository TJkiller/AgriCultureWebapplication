@model IEnumerable<AgriCultureWeb.Models.Employee>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Index</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa; /* Light grey background for less strain on the eyes */
            color: #212529; /* Bootstrap's default text color for better readability */
            font-family: 'Segoe UI', Arial, sans-serif; /* More modern font-family */
        }

        .primary-button {
            background-color: #0d6efd; /* Bootstrap primary blue */
            color: white;
            border: none;
            border-radius: 0.25rem;
            padding: 0.375rem 0.75rem; /* Standard Bootstrap padding */
            text-decoration: none;
        }

            .primary-button:hover {
                background-color: #0b5ed7; /* A slightly darker blue on hover */
                color: white;
            }

        .table-container {
            margin-top: 40px;
            background-color: white;
            padding: 20px;
            border-radius: 0.25rem;
            box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075); /* Subtle shadow for depth */
        }

        h1 {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1>Create New Farmer Profile</h1>

        <p>
            <a href="@Url.Action("Create")" class="primary-button">Add New Profile</a>
        </p>

        <div class="table-container">
            <table class="table table-striped">
                <thead class="table-dark">
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Name)</th>
                        <th>@Html.DisplayNameFor(model => model.Email)</th>
                        <th>@Html.DisplayNameFor(model => model.Password)</th>
                        <th>@Html.DisplayNameFor(model => model.ConfirmPassword)</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.Name)</td>
                            <td>@Html.DisplayFor(modelItem => item.Email)</td>
                            <td>@Html.DisplayFor(modelItem => item.Password)</td>
                            <td>@Html.DisplayFor(modelItem => item.ConfirmPassword)</td>
                            <td>
                                <a href="@Url.Action("Edit", new { id = item.Id })" class="primary-button">Edit</a>
                                <a href="@Url.Action("Details", new { id = item.Id })" class="primary-button">Details</a>
                                <a href="@Url.Action("Delete", new { id = item.Id })" class="primary-button">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <p class="mt-4">
        <a href="/Farmers/Get" class="primary-button"><i class="bi bi-list"></i> View Farmers Products</a>
    </p>
</body>
</html>
